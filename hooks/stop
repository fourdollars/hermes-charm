#!/bin/bash
# stop hook — stop the Hermes Gateway service
# shellcheck disable=SC1091

source "$(dirname "$0")/common.sh"

status_set maintenance "Stopping Hermes Gateway"

stop_service

log "Hermes Gateway stopped"
status_set maintenance "Stopped"
